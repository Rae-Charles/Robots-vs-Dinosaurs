from fleet import Fleet
from herd import Herd

class Battlefield:
    def __init__(self):
        self.fleet = Fleet()
        self.herd = Herd()

    def __init__(self):
        print("Welcome to Robots vs Dinosuars.")

    def run_game(self):
        health = > 0
        keep_running_scenario = True

        while keep_running_scenario is True:
            if health Fleet() == > 0 
                run_game = True
            elseif: 
            if health Herd() == >0
                rung_game = True
            else: 
                #stop running game     
